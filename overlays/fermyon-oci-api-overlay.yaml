overlay: 1.0.0
info:
  title: API Evangelist enhancements — Fermyon oci-api API
  version: 1.0.0
extends: fermyon-oci-api-openapi.yml
x-generated: '2026-09-09'
x-method: generated
x-source: API Evangelist enrichment pipeline; targets openapi/fermyon-oci-api-openapi.yml
x-note: Fermyon publishes the Cloud API contract without a single operationId (verified against https://raw.githubusercontent.com/fermyon/cloud-openapi/main/swagger.json
  on 2026-09-09). These operationIds are computed deterministically from HTTP method + path by API Evangelist and
  are NOT names Fermyon publishes. They are kept in this overlay rather than written into the spec so the provider
  contract stays unmodified.
actions:
- target: $.paths['/api/oci'].get
  description: Add a stable operationId for GET /api/oci; the published contract declares none.
  update:
    operationId: getOci
- target: $.paths['/api/oci/{name}/manifests/{reference}'].put
  description: Add a stable operationId for PUT /api/oci/{name}/manifests/{reference}; the published contract declares
    none.
  update:
    operationId: replaceOciByNameManifestsByReference
- target: $.paths['/api/oci/{name}/blobs/uploads'].post
  description: Add a stable operationId for POST /api/oci/{name}/blobs/uploads; the published contract declares
    none.
  update:
    operationId: createOciByNameBlobsUploads
- target: $.paths['/api/oci/{name}/blobs/uploads/{digest}'].get
  description: Add a stable operationId for GET /api/oci/{name}/blobs/uploads/{digest}; the published contract declares
    none.
  update:
    operationId: getOciByNameBlobsUploadsByDigest
- target: $.paths['/api/oci/{name}/blobs/uploads/{digest}'].put
  description: Add a stable operationId for PUT /api/oci/{name}/blobs/uploads/{digest}; the published contract declares
    none.
  update:
    operationId: replaceOciByNameBlobsUploadsByDigest
- target: $.paths['/api/oci/{name}/blobs/uploads/{digest}'].patch
  description: Add a stable operationId for PATCH /api/oci/{name}/blobs/uploads/{digest}; the published contract
    declares none.
  update:
    operationId: updateOciByNameBlobsUploadsByDigest
- target: $.paths['/api/oci/{name}/blobs/uploads/{digest}'].delete
  description: Add a stable operationId for DELETE /api/oci/{name}/blobs/uploads/{digest}; the published contract
    declares none.
  update:
    operationId: deleteOciByNameBlobsUploadsByDigest
- target: $.info
  description: Attach the provider documentation and contract source that the published document omits.
  update:
    contact:
      name: Fermyon Cloud documentation
      url: https://developer.fermyon.com/cloud/index
- target: $
  description: Attach externalDocs pointing at the Fermyon Cloud REST API reference page.
  update:
    externalDocs:
      description: Fermyon Cloud REST API reference
      url: https://developer.fermyon.com/cloud/rest-api
